// Left rail: near-black in light mode; in dark mode it defers to the app's own
// dark --sidebar tokens instead of staying pinned to one look. See .rail-surface
// in globals.css.
export const RAIL_SURFACE_CLASS = 'rail-surface';
